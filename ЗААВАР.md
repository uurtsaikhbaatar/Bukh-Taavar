# Монгол бөхийн таавар — ЗААВАР

> Найз нөхдийн хаалттай хүрээнд монгол бөхийн барилдаан/тэмцээний үр дүн дээр
> **үүсмэл токеноор** таавар тавьж, **магадлал** харж, самбараар өрсөлдөх веб апп.
> Обсидиан: `Миний төслүүд\22. Монгол бөхийн таавар\`.

| Үе шат | Юу | Төлөв |
|---|---|---|
| Phase 1 | Цөм: LMSR зах зээл, event-sourced токены дэвтэр, Elo магадлал, devjee клиент, тест/симуляци | ✅ |
| Phase 2 | Веб сервер (API, SSE, имэйл бүртгэл, Postgres), Expo/RN-Web апп, devjee автомат sync, Docker/Render | ✅ код бэлэн, **deploy хийгдээгүй** |
| Phase 3 | 1xbet маягийн боломжууд: Монте-Карло прогноз + «хэд давах / хэн холдох / аварга» автомат зах зээл, экспресс/систем/Lucky/Patent купон, Наадмын тото (60/30/10), децимал коэффициент, max stake, өдрийн бонус, найз урих бонус, кэш-аут | ✅ 2026-08-17 (63 тест) |

## Бүтэц

| Хэсэг | Юу |
|---|---|
| `src/` | Цөм (Phase 1): `lmsr` · `rating` · `engine` · `state`/`events`/`store` · `devjee` · `csv` · `format` |
| `server/` | Веб: `index.ts` (эхлүүлэх) · `api.ts` (endpoint) · `http.ts` (router/cookie/SSE/статик) · `auth.ts`+`auth-store.ts` (scrypt, session, код) · `email.ts` (Brevo/консол) · `db.ts` (Postgres лог+бүртгэл) · `devjee-sync.ts` · `admin-cli.ts` |
| `app/` | Expo SDK 57 / React Native Web апп: `App.tsx`, `src/screens/*` (Auth, Home, Market, Portfolio+Leaderboard, Admin), `src/shared/api.ts` (сервертэй хамтын DTO) |
| `scripts/` | `demo`, `sim`, `devjee-demo`, `calibrate`, `import-csv` |
| `data/` | Локал лог `web-events.jsonl`, бүртгэл `web-auth.json`, devjee кэш — git-д ордоггүй |
| `Dockerfile`, `render.yaml`, `.env.example` | Deploy |

**Dependency:** цөм 0; сервер `pg` л; апп — expo/react/react-native (bigtwo-той ижил хувилбар). Node 24.

**Нэр томьёо (эзний шийдвэр, 2026-08-17):** «барилдаан» (ноцолдоон гэж хэлэхгүй); давсан бөх — «давсан/давалт/давагч», унасан бөх — «унасан» («хожил/хожсон» гэж хэлэхгүй). Купоны төлбөрт «давбал … хүртэл», дэвтэрт «Давалт». Код дахь нэр (`bout`, `won`) англиар хэвээр.

**Таамгийн калибровк (2026-09-06):** барилдааны магадлал = `predictProbability` (`src/rating.ts`) — `z = 2.0267·(Δрейтинг/400) + 0.2469·ln((1+тА)/(1+тБ)) − 0.1719·ln((1+аА)/(1+аБ))`, т = бодит барилдааны тоо (туршлага), а = сүүлд барилдснаас хойшх хоног (≤3 жил — удаан завсарласан нь сул). Архивын 246 845 барилдааныг он цагаар гүйж (look-ahead хаалттай), ≤2023-оор сургаад 2024/2025/2026 дээр шалгасан: log-loss **0.5186 → 0.5044** (жил бүрд сайн); жинг бүх өгөгдлөөр эцэслэсэн. Тохируулга: `npm run calib` (`scripts/calib-tune.ts` — M1 α, M2 α(g), M3 logistic хувилбаруудыг зэрэг хэмждэг). Рейтингийн **шинэчлэл** хэвээр K=32/400 (`winProbability`) — devjee масштаб өөрчлөгдөөгүй. Туршлага/огноо `RatingEntry.games/lastBoutAt`-д хөтлөгдөнө: архивын импорт тавьж өгнө, барилдаан бүр +1 (гоц тооцохгүй); прогноз (МК) ижил калибровктой (амралт зөвхөн симийн 1-р даваанд). `/health` одоо `storage/email/archive` талбартай.

**devjee sync:** тэмцээний дэлгэц (Админ → Тэмцээн → тэмцээн) дээр «devjee sync — автомат шинэчлэл» карт: асаалттай бол 30 сек тутам devjee.mn-ээс төлөв татаж шинэ хослол → зах зээл нээх, давсан бөх → шийдэж төлбөр тараах, гоц → хүчингүй/буцаалт; «Одоо sync» / «Sync асаах/унтраах». Асаалттай төлөв Engine-д (`Tournament.syncEnabled`, үйл явдал) хадгалагддаг тул сервер дахин ассан ч (Render deploy) sync унтрахгүй; **тэмцээн дуусмагц (аварга тодорсон) sync автоматаар унтарна** (2026-09-06). Аварга = сүүлийн даваанд давсан **унаагүй** бөх (сүүлийн даваанд нэмэлт цолын/3-р байрын барилдаан байсан ч зөв).

## Командууд (PowerShell, `cd D:\Claude\Claude\Projects\Apps\bukh-taavar`)

| Команд | Юу | Сүлжээ |
|---|---|---|
| `npm run setup` | Бүх dependency суулгах (эхний удаа: root + app) | тийм |
| `npm run check` | typecheck + **65 тест** (цөм 56 + сервер 9) | үгүй |
| `npm run build:web` | Аппыг вэбд бүтээнэ → `app/dist` (сервер эндээс үйлчилнэ) | үгүй |
| `npm run web` | **Локал сервер** http://localhost:8787 — JSONL лог, консол имэйл (код серверийн логд хэвлэгдэнэ) | devjee-д л |
| `npm run admin -- users` | Хэрэглэгчид, үлдэгдэл | үгүй |
| `npm run admin -- make-admin <нэр>` · `grant <нэр> <тоо>` · `reset-pw <нэр> <нууц үг>` · `verify <нэр>` | Админы үйлдлүүд (production-д `DATABASE_URL` тавиад) | — |
| `npm run demo` / `npm run sim` / `npm run devjee` / `npm run calibrate` | Phase 1-ийн туршилтууд | sim/demo үгүй; devjee/calibrate тийм |
| `npm run archive` | **devjee.mn-ийн БҮХ түүхийг татна** → `data/devjee/` (2026-09-05: 1 457 тэмцээн, 247 142 барилдаан, 22 360 бөх; анх ≈11 мин, кэштэй — дахин ажиллуулбал зөвхөн шинийг татна, ≈7 мин) | тийм |
| `npm run h2h -- "Орхонбаяр" "Батмагнай"` | Хоёр бөхийн **хоорондын харьцаа** + **10 үеийн давамгайллын гинж** (замуудтай) + Bradley–Terry/Elo магадлал. Нэр давхардвал жагсаалт гаргана; `--wid` өгч болно | үгүй (архив) |
| `npm run eval` / `npm run tune` | Загваруудын backtest (2026 оны 10 665 барилдаан дээр) / гинжийн параметр тохируулга | үгүй |
| `npm --prefix app run web` | Expo dev сервер (8081) — hot reload; API-г 8787 руу өөрөө чиглүүлнэ | — |

> Локал шалгалт хийсэн (2026-08-16): бүртгэл → консолын кодоор баталгаажуулалт → devjee-ээс тэмцээн импорт (76 бөх) → sync (63 барилдаан, шийдэгдсэн) → гараар барилдаан нээх → 1 000 токен таавар (51%→70%) → хагасыг зарах → админ үр дүн бүртгэх → төлбөр (SSE-ээр толгойн үлдэгдэл шинэчлэгдэнэ) → самбар/дэвтэр.

## Phase 3 — 1xbet маягийн бооцооны боломжууд (2026-08-16)

| Юу | Хаана | Дүрэм |
|---|---|---|
| **Тэмцээний прогноз** (Монте-Карло, 500 симуляци) | Админ → Тэмцээн → «Прогноз» карт; `/api/tournaments/:id/forecast` | Оролцогчид (devjee импортын зэрэг дэв, 2^даваа) + Elo; даваа бүрд санамсаргүй хослол; мэдэгдсэн үр дүнгээр нөхцөлдүүлнэ |
| **Хэд давах** (N.5 дээш/доош, яг хэд), **хэн холдох** (А/Б/тэнцүү), **аварга хэн** (топ-K + Бусад) | Админ → Тэмцээн → «Загварууд» (А/Б бөх сонгоод товч); `/api/admin/markets/rule` | Анхны магадлал прогнозоос; **автоматаар шийдэгдэнэ** (бөх унасан/аварга тодорсон); гоц = давсан; оролцоогүй бол хүчингүй |
| **Экспресс, систем, Lucky, Patent** | Зах зээлийн хуудас «＋ купон» → доод «Купон» самбар → Купон дэлгэц | Коэффициент = сонголтуудын магадлалын үржвэр (тавих үеийнх, тогтмол); ≤ 8 сонголт; давалт ≤ 100× (хослол бүрд); купон ≤ 5 000; нэг бөхтэй холбоотой хоёр сонголт хориотой; хүчингүй хөл хасагдана; систем m/n = m-ийн бүх хослолд тэнцүү хуваана |
| **Тото (пул)** | «🎟 Тото» таб; админ Тэмцээн → «Тото үүсгэх» (хүлээгдэж буй ≤16 барилдаан, хураамж 100) | Барилдаан эхэлмэгц хаагдана; бүгд дуусахад автомат: зөв таасан тоогоор эрэмбэлж пулыг **60/30/10** шатлан хуваана (шатанд олон бол тэнцүү, үлдэгдэл дээд шатанд); хэн ч таагаагүй → бүгдэд буцаалт |
| Децимал коэффициент | Зах зээл: `×1.61 · загвар 62%` | 1/магадлал (спот); бодит үржүүлэгч санал дээр |
| Дээд бооцоо | Engine `maxBet` 5 000, купон 5 000 | |
| Өдөр тутмын урамшуулал | «Би» → «+200» (үлдэгдэл < 5 000, 20 цагт нэг) | `/api/bonus` |
| Найз урих | Бүртгүүлэхэд «Хэн урьсан» — хоёуланд +1 000 | `/api/register {referrer}` |
| «Кэш-аут» | Зарах = кэш-аут (дэвтэрт нэршил) | |

## Phase 3 — бооцооны боломжууд (1xbet-ийн санаанууд бөхөд)

| Боломж | Хэрхэн | Хаана |
|---|---|---|
| **Прогноз (Монте-Карло)** | Тэмцээний оролцогчдын Elo-гоор 500 симуляци: даваа бүрд амьд үлдэгсдийг санамсаргүй хослуулна, мэдэгдсэн үр дүнгүүдээр нөхцөлдүүлнэ → бөх бүрийн «хэд давах» тархалт, аваргын магадлал | `src/forecast.ts`, Админ → Тэмцээн → «Прогноз» карт; `GET /api/tournaments/:id/forecast` |
| **Хэд давах (total)** | «X N+ даваа давах уу» (2 үр дүн) / «яг хэд» (0..rounds); анхны магадлал прогнозоос; тэмцээний явцаас **автомат шийдэгдэнэ** (унасан → тодорхой; гоц = давсан) | Админ → Тэмцээн → «Загварууд» (А бөх сонгоод босго өгнө); `POST /api/admin/markets/rule` |
| **Хэн холдох (matchup)** | [А, Б, Тэнцүү] — аль нь илүү олон даваа давах | мөн адил |
| **Аварга (outright)** | Топ-K + «Бусад» — прогнозоор эрэмбэлж, финалын үр дүнгээр шийднэ | Прогноз карт → «Аварга зах зээл нээх» |
| **Экспресс** | Зах зээлийн хуудасны «＋ купон» → доод «🧾 Купон» самбар → бүх сонголт таарвал коэффициентүүд үржинэ (тогтмол, LMSR хөдөлгөхгүй, хаус даана); ≤ 8 сонголт, давалт ≤ 100×, купон ≤ 5 000; нэг бөхтэй холбоотой давхар сонголт хориотой; хүчингүй хөл хасагдана | `Engine.quoteCoupon/placeCoupon`, `/api/coupons` |
| **Систем / Lucky / Patent** | Систем k/n — k сонголттой бүх хослолд тэнцүү; Lucky — 1..n; Patent — 2..n; хослол тус бүр тусдаа тооцогдоно | Купон дэлгэц дээрх сонголт |
| **Тото (пул)** | Админ N барилдаан сонгож үүсгэнэ (хураамж 100); оролцогч давагчдыг таана; эхний барилдаан шийдэгдэхэд хаагдана; бүгд дуусахад **60/30/10** шатлан хуваана (зөв таасан тоогоор бүлэглэж), хэн ч таагаагүй бол буцаалт | «🎟 Тото» таб; Админ → Тэмцээн → «Тото үүсгэх»; `Engine.createToto/enterToto/settleToto` |
| **Децимал коэффициент** | Магадлалын хажууд ×1.61 (спот 1/p); саналд бодит үржүүлэгч | Зах зээлийн хуудас |
| **Хязгаар** | Нэг бооцоо ≤ 5 000 (`maxBet`), купон ≤ 5 000, давалт ≤ 100× | `Engine` тохиргоо |
| **Урамшуулал** | Өдөр тутам +200 (үлдэгдэл 5 000-аас доош, 20 цагт нэг) — «Би» таб; найз урих: бүртгүүлэхдээ найзын нэрийг бичвэл хоёуланд +1 000 | `/api/bonus`, `/api/register {referrer}` |
| **Кэш-аут** | Эзэмшлээ хэзээ ч зарах (LMSR үнээр) — 1xbet cash-out-той адил | Зах зээлийн хуудас |

Хийгээгүй (санаатай): гинжин/болзолт/анти-экспресс (төвөгтэй, бага үнэ цэнэ), барилдааны доторх амьд бооцоо (үйл явдлын явц байхгүй).

## Бооцооны самбар ба даваа удирдах (2026-08-17)

| Юу | Хаана | Хэрхэн |
|---|---|---|
| **Бооцооны самбар** (хосоор) | Нүүр «Зах зээл» → тэмцээний карт → «🥋 Барилдаанууд → бооцоо тавих»; `GET /api/tournaments/:id/board?round=` | Тухайн давааны **бүх хос** нэг дэлгэцэд: бөх бүр (нэр · цол · нутаг · зах зээлийн % · ×коэфф. · загварын %). А/Б дээр дарж сонгоно → мөрөнд дүн (100/500/1 000/5 000/өөрөө, бодит санал «давбал N (×k)») → доод «Тавих (N)» нэг товчоор бүгдийг тавина (тус бүр **дан** бооцоо; `POST /api/markets/buy-batch {orders[]}`, нэг нь алдвал бусад нь тавигдана, requestId идемпотент). Даваа сонгох чип (✓ = дууссан), хайлт (нэр/цол/аймаг), «Миний» шүүлт; дууссан хосод «✓ давсан», миний хувь/төлбөр; толгойд «🧾 Миний бооцоо — N-р даваа: X барилдаан · тавьсан · буцаж ирсэн (давсан/унасан) · ашиг» — даваа дуусахад төлбөр үлдэгдэлд орсныг шууд харуулна. «Дэлгэрэнгүй ›» → хуучин зах зээлийн хуудас (харьцаа, кэш-аут). Нүүрэнд барилдааны зах зээл тус бүрээр гарахаа больсон — зөвхөн дүрэмт/бусад. |
| **Туршилтын тэмцээн үүсгэх** | Админ → Тэмцээн → «Шинэ тэмцээн» → «Туршилтын тэмцээн үүсгэх (N бөх)»; `POST /api/admin/tournaments/generate {size=1024, name?, activeSince='2024-01-01', levels='aimag,uls'}` | Улс/аймгийн цолтой, 2024 оноос хойш барилдсан (архивын огноо) бөхчүүдээс рейтингээр топ N → `entrants` (зэрэглэлийн дараалал), даваа = log2(N) (1024 → 10), kind «туршилт». |
| **Даваа эхлүүлэх** | Админ → тэмцээн → «Даваа удирдах» → «▶ N-р давааг эхлүүлэх»; `POST /api/admin/tournaments/:id/rounds/:n/start {pairing:'rank'|'random'|'seq'}` (`Engine.startRound`) | Амьд (унаагүй, өмнөх даваанд давсан/гоц) бөхчүүдийг хослуулж барилдаан + LMSR зах зээл нээнэ. `rank` (анхдагч) = наадмын оноолт шиг: зэрэглэлээр эрэмбэлж дээд хагас ↔ доод хагас (1↔513 …); `random` санамсаргүй. Сондгой бол хамгийн дээд зэрэглэлийнх гоц. Зөвхөн ээлжит давааг (өмнөх нь бүрэн дууссан үед). 512 барилдаан ≈ 1 сек. |
| **Даваа дуусгах** | Гол товч **«⏭ N-р давааг дуусгаад N+1-р давааг эхлүүлэх»** (хоёр дарж баталгаажуулна; финалд «Финалыг дуусгах — аварга тодруулах»), эсвэл «■ Зөвхөн дуусгах»; `POST …/rounds/:n/finish {mode:'simulate'|'favorite'}` (`Engine.finishRound`). Карт дээр «Одоо: N-р даваанд бооцоо авч байна — X барилдаан, нийт Y токен бооцоо» + дараагийн алхмын тайлбар | Хүлээгдэж буй бүх барилдааны үр дүн: `simulate` (анхдагч) — давсан бөхийг Elo магадлалаар санамсаргүй сугална (туршилт; ~16% гэнэтийн үр дүн), `favorite` — рейтинг өндөр нь. Барилдаан бүр `recordBoutResult` → зах зээл шийдэж төлбөр, дүрэмт зах зээл/тото автоматаар, Elo шинэчлэгдэнэ. Тодорхой барилдааныг өмнө нь гараар бүртгэж болно. 512 барилдаан ≈ 1.2 сек. Бодит (devjee) тэмцээнд энэ карт гарахгүй — sync ажиллана. |
| **Давааны төлөв** | `Engine.roundStatus` → `TournamentDto.currentRound/currentPending/currentTotal/finished/championName/entrants`, `RoundStatusDto` (самбар, админ) | |
| **Бөх хайлт** | Админ → Бөх → Хайх; тэмцээний «Бөх хайх»; самбарын хайлт; `GET /api/wrestlers?q=` | Үг бүр нэр / овог нэр / цол / аймаг / сум-ын аль нэгэнд таарах ёстой (AND): «улсын заан ховд», «сумъяа алтай», «аймгийн арслан». |

Локал туршилт (2026-08-17): «Туршилтын их наадам 2026 — 1024 бөх» (1 319 нийцсэн бөхөөс топ 1024) → 1-р даваа 512 барилдаан (0.9 сек) → самбараас 2 бооцоо нэг дор → 1-р давааг дуусгах (1.2 сек, 512 зах зээл шийдэгдсэн, 81 гэнэтийн үр дүн) → 2-р даваа 256 барилдаан нээлттэй. Лог +≈2.5к үйл явдал/даваа.

## Хэрэглэгчийн урсгал

1. **Бүртгүүлэх** (нэр, имэйл, нууц үг) → **эхний хэрэглэгч автоматаар админ**. Имэйлээр 6 оронтой код ирнэ → «Би» хэсэгт оруулж баталгаажуулна (баталгаажуулаагүй бол зөвхөн харна, таавар тавихгүй).
2. **Зах зээл** таб: тэмцээнээр бүлэглэсэн нээлттэй зах зээлүүд — тод зурвас = зах зээлийн магадлал, цагаан тэмдэг = загварын (Elo) магадлал.
3. **Зах зээл нээх** → үр дүн сонгож токен оруулна → амьд санал (хувь, давбал, ×, магадлалын өөрчлөлт) → «тавих». Эзэмшлээ хагас/бүгдийг зарж болно.
4. **Би** — үлдэгдэл, эзэмшил, дэвтэр, нууц үг солих, гарах. **Самбар** — нийт = үлдэгдэл + эзэмшлийн үнэлгээ.
5. **Админ**: *Тэмцээн* (гараар үүсгэх, барилдаан нэмэх → зах зээл шууд нээгдэнэ, үр дүн бүртгэх → төлбөр, олон үр дүнтэй зах зээл, хаах/шийдэх/хүчингүй) · *Бөх* (нэмэх, хайх) · *Хэрэглэгч* (токен олгох, админ болгох) · *devjee* (тэмцээн импорт → «Sync асаах» → 30 сек тутам шинэ хослол → зах зээл, үр дүн → шийдвэр, гоц → хүчингүй).

## Deploy (Render + Neon) — `bukh.daidi13.com` дэд домэйнээр

Тусдаа Render үйлчилгээ (Дай Ди-гийн сервертэй холихгүй — өөр стек, өөр DB, өөр auth); daidi13.com-ын **дэд домэйн** (`bukh.daidi13.com`) руу CNAME-ээр заана. Нууц утгуудыг (DATABASE_URL, Brevo key) зөвхөн Render Dashboard дээр оруулна — чат/файлд хэзээ ч биш.

1. **GitHub**: хоосон private repo үүсгэнэ (жишээ `bukh-taavar`) → энэ хавтсыг (`bukh-taavar` = repo-гийн үндэс) `git init` + commit + push. `archive/` (devjee архив gzip ≈10 MB: `npm run archive:pack`) repo-д орно — Docker build задалж `data/devjee`-д тавина, тиймээс production дээр хоорондын харьцаа, «Бүх бөхийг оруулах», архивын Elo, 1024 бөхийн тэмцээн бүгд ажиллана. `data/`, `.env` орохгүй.
2. **Neon**: bigtwo-ийн Neon төсөл дотор шинэ database (эсвэл шинэ төсөл) → `DATABASE_URL` (`?sslmode=require`). Хүснэгтүүдийг сервер өөрөө үүсгэнэ (`SCHEMA_SQL`).
3. **Brevo**: bigtwo-ийн ижил API key; `EMAIL_FROM="Бөхийн таавар <noreply@daidi13.com>"` (илгээгч домэйн Brevo дээр аль хэдийн баталгаажсан).
4. **Render** → New → Blueprint → repo сонгоно → `render.yaml` уншина (Docker, Singapore, `/health`) → Environment: `DATABASE_URL`, `EMAIL_PROVIDER=brevo`, `EMAIL_API_KEY`, `EMAIL_FROM` (SECURE_COOKIES=1, NODE_ENV render.yaml-д бий) → Deploy (≈5–8 мин: Expo build + архив задлах).
5. **Домэйн**: Render → Settings → Custom Domains → `bukh.daidi13.com` нэмнэ → Render-ийн заасан CNAME (`<нэр>.onrender.com`) утгыг daidi13.com-ын DNS дээр (bigtwo-ийн домэйн хаана удирдагддаг — тэнд) нэмнэ → SSL автомат (хэдэн минут).
6. Нээгээд **эхний бүртгэл = админ** (uuree-ээр бүртгүүл) → имэйл код → Админ → Бөх → «Бүх бөхийг оруулах» (архиваас 21 870 бөх, ≈40 сек; Neon руу нэг INSERT-ээр багцалж бичдэг) → devjee таб/туршилтын тэмцээн.
7. Төлөвлөгөө: **free** — 15 мин ажилгүй бол унтдаг (эхний нээлт ~30–60 сек, унтсан үед devjee sync ажиллахгүй!) → бодит тэмцээний өдөр **starter ($7/сар, унтардаггүй)**. RAM: архив + Engine ≈ 130 MB heap (RSS ≈ 350 MB, `NODE_OPTIONS=--max-old-space-size=384`) — 512 MB-д багтана; хүрэлцэхгүй бол Standard (2 GB) эсвэл архивыг шахах.

**Санамж:** локал `data/` лог/бүртгэл production-д хамаагүй (Postgres дээр шинээр эхэлнэ — локалын туршилтын тэмцээн, үлдэгдэл шилжихгүй). Postgres дээр events хүснэгт = бүх түүх; Neon-ы backup-ыг асаана уу. `npm run mem` — санах ойн хэмжилт.

**devjee амьд:** devjee.mn-ийн `states3/{tid}` баримт тэмцээний үеэр талбай дээрээс шууд шинэчлэгддэг (даваа бүрийн хослол → бидэнд барилдаан+зах зээл; барилдаан эхлэхэд `startedAt` → зах зээл **хаагдана** (амьд барилдаанд бооцоо авахгүй); давсан бөх бичигдэхэд → шийдэж төлбөр; гоц → хүчингүй). Sync асаалттай бол 30 сек тутам — админ юу ч дарахгүй, даваанууд өөрөө үргэлжилнэ. «Даваа удирдах» гар удирдлага зөвхөн devjee-гүй (туршилт/гараар) тэмцээнд.

## Механизм товчхон

- **Магадлал = зах зээлийн үнэ (LMSR).** Анхны үнэ = devjee Elo (`P = 1/(1+10^((R_B−R_A)/400))`); рейтинггүй бөхөд цолын суурь. Таавар тавих бүрд үнэ хөдөлнө; давсан хувь = 1 токен.
- **b (хөрвөх чадвар)** анхдагч 2 000: 50/50-аас 1 000 токен тавьбал ~70%. Олон үр дүнтэй зах зээлд 4 000.
- **Токен бүхэл, хувь бутархай.** Авах ≥ 10; зарах орлого доош тоймлоно; төлбөр = round(хувь). Хүчингүй = цэвэр зарцуулалт буцна.
- **Хаусын алдагдал** ≤ b·ln(1/p₀) — виртуал тул хамаагүй, тестээр баталгаажсан.
- **Идемпотент** requestId, **rate limit** (нэвтрэлт 12/мин IP-д, арилжаа 6/2 сек), session cookie HttpOnly SameSite=Lax + Bearer.
- **Инвариант:** Σ үлдэгдэл + хаусын кэш = Σ оруулсан; үлдэгдэл ≥ 0; магадлалын нийлбэр 1.

## devjee.mn ба архив

МҮБХ-ны албан ёсны бүртгэл/оноолтын программ (Elo K=32, чансаа). Бүрэн судалгаа: Обсидиан `22.../Inputs/devjee.mn — сайтын судалгаа.md`. Манай зарчим: хөнгөн, кэштэй (`data/cache/devjee`), тэмцээний өдөр л sync. API өөрчлөгдвөл `src/devjee.ts`, `server/devjee-sync.ts`.

**devjee API-ийн өөрчлөлт (2026-09):** сайт шинэчлэгдэж тэмцээний амьд төлөв Firestore `states3`-аас **`api.devjee.mn/tournament-state/snapshot?id={tid}`** руу нүүсэн (ижил zlib+base64 `data`; live update нь RTDB `_actions/{tid}`-ээр). `DevjeeClient.tournamentState` одоо snapshot-ыг түрүүлж үзээд, бүтэлгүйтвэл/хоосон бол states3 руу буцдаг — хуучин `states3` баримтууд цэвэрлэгдсэн байж болзошгүй (200 OK ч хоосон ирдэг). Дахин өөрчлөгдвөл: bundle-ийг нь татаж `grep -oE 'https://[a-z.-]+' index-*.js` + collection enum-ыг хайх.

**Архив** (`npm run archive`, 2026-08-16, сүүлд 2026-09-05): 1 457 тэмцээн (1900→2026, бүгд төлөвтэй), **247 142 барилдаан** (devjee-ийн ~441к «id» нь барилдаан бүрд 2 бичлэг тул ижил хэмжээ), **22 360 бөх** — овог нэр, цол, аймаг/сум, төрсөн он, өндөр (5 060), жин (1 462), дэвжээ (2 600), харьяалал (7 228), цолын түүх (7 398), идэвхтэй эсэх (`--refresh-wrestlers` кэшээс сүлжээгүй дахин үүсгэнэ). Сервер эхлэхэд архив байвал ачаалж (`server/analytics.ts`, ~1 сек), зах зээлийн хуудсанд «Хоорондын харьцаа» карт + `/api/h2h?a=&b=` гарна.

**Аппын бөхийн сан:** Админ → Бөх → «Бүх бөхийг оруулах» (`POST /api/admin/import-archive`) — архивын 21 870 бөхийг Engine-д оруулж (id = devjee wid), рейтингийг архивын Elo replay-ээр (суурь **1300** → devjee-ийн тоотой ±10, нэг масштаб) тавина; идемпотент (дахин дарахад дутууг л нөхнө). Нэг удаа ≈40 сек, лог +≈45к үйл явдал. Хийгдсэн 2026-08-17 (локал).

**Загваруудын backtest** (`npm run eval`: сургалт < 2026-01-01, шалгалт 2026 оны 10 665 барилдаан; санамсаргүй log-loss 0.693):

| Загвар | log-loss | Дүгнэлт |
|---|---|---|
| Elo replay (K=32, цолын суурь, онлайн) | **0.494** | Хамгийн сайн — devjee-ийн Elo-той ижил арга; аппын анхны магадлалд үүнийг (devjee-ийн рейтинг) хэрэглэнэ |
| Bradley–Terry (бүх граф, ½ задрал 4 жил) | 0.526 | Калибровк сайн; статик тул онлайн Elo-гоос бага зэрэг сул |
| Цолын суурь (зөвхөн цол) | 0.599 | Рейтинггүй бөхийн нөөц |
| Давамгайллын гинж (10 үе, сэлбэсэн) | 0.68 | Таамагт сул (шууд цөөн барилдаан давамгайлна) — **тайлбарт** (хэн хэнийг давсан гинж) хэрэглэнэ, магадлалд биш |

Гинжийн санаа (А давсан хүнийг давсан хүн … Б) нь BT/Elo-д аль хэдийн «харилцан хамааралтайгаар» багтдаг — тэдгээр нь бүх урттай гинжийг статистикийн зөв жинтэй нэгтгэдэг.

## Дараагийн боломжит алхмууд
Push/Telegram мэдэгдэл · «X хэд давах» зах зээл автомат нээх · зэрэг дэв/чансааны дохиог загварт нэмэх · Наадмын өдрийн ачааллын тест · утасны апп (Expo native).
